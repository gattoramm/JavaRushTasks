package com.javarush.task.task02.task0212;

public class Solution {
    public static void main(String[] args) {
        // создаем переменную cat1 типа Cat (Cat cat1) и объект Cat (new Cat())
        // и присваиваем переменной ссылку на объект
        Cat cat1 = new Cat();
        // создаем переменную cat2 типа Cat (Cat cat2) и объект Cat (new Cat())
        // и присваиваем переменной ссылку на объект
        Cat cat2 = new Cat();
        // создаем переменную cat3 типа Cat
        Cat cat3;
        // создаем переменную cat4 типа Cat (Cat cat4) и объект Cat (new Cat())
        // и присваиваем переменной ссылку на объект
        Cat cat4 = new Cat();
        // создаем переменную cat5 типа Cat (Cat cat5) и объект Cat (new Cat())
        // и присваиваем переменной ссылку на объект
        Cat cat5 = new Cat();
        // создаем переменную cat6 типа Cat (Cat cat6) и объект Cat (new Cat())
        // и присваиваем переменной ссылку на объект
        Cat cat6 = new Cat();
        // создаем переменную cat7 типа Cat (Cat cat7) и объект Cat (new Cat())
        // и присваиваем переменной ссылку на объект
        Cat cat7 = new Cat();
        // создаем переменную cat8 типа Cat (Cat cat8) и объект Cat (new Cat())
        // и присваиваем переменной ссылку на объект
        Cat cat8 = new Cat();
        // создаем переменную cat9 типа Cat (Cat cat9) и объект Cat (new Cat())
        // и присваиваем переменной ссылку на объект
        Cat cat9 = new Cat();
        // создаем переменную cat10 типа Cat
        Cat cat10;
    }


    // class Cat
    public static class Cat {

    }
}
